# In local development Vite proxies /api and /uploads to this target.
VITE_PROXY_TARGET=http://localhost:5000

# Optional. Leave empty to use the same origin (/api).
# Example for a separately hosted frontend:
# VITE_API_URL=http://localhost:5000/api
VITE_API_URL=
